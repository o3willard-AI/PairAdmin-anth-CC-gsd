---
phase: 05-settings-configuration-slash-commands
plan: 04
type: execute
wave: 3
depends_on: ["05-02", "05-03"]
files_modified: []
autonomous: false
requirements:
  - CFG-01
  - CFG-02
  - CFG-03
  - CFG-07
  - SLASH-01
  - SLASH-07
  - SLASH-08

must_haves:
  truths:
    - "Settings dialog opens from gear icon and all 5 tabs are navigable"
    - "LLM Config tab saves provider/model and API key to keychain"
    - "Connection test shows inline success or error"
    - "Slash commands /model, /theme, /help produce correct output in chat"
    - "StatusBar reflects active model after /model or settings save"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete settings and slash command system.

Purpose: Confirm end-to-end functionality of the settings dialog, slash commands, and StatusBar integration with a live Wails application.

Output: Verified working settings and slash command system.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-settings-configuration-slash-commands/05-01-SUMMARY.md
@.planning/phases/05-settings-configuration-slash-commands/05-02-SUMMARY.md
@.planning/phases/05-settings-configuration-slash-commands/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and fix any integration issues</name>
  <files></files>
  <read_first>
    services/settings_service.go,
    services/config/config.go,
    services/commands.go,
    frontend/src/components/chat/ChatPane.tsx,
    frontend/src/components/settings/SettingsDialog.tsx,
    frontend/src/components/layout/StatusBar.tsx,
    frontend/src/components/layout/ThreeColumnLayout.tsx,
    main.go
  </read_first>
  <action>
    Run the full test suite:
    ```bash
    cd /home/sblanken/working/ppa2 && go build ./... && go test ./... -count=1 2>&1 | tail -30
    cd /home/sblanken/working/ppa2/frontend && npx vitest run --reporter=verbose 2>&1 | tail -30
    ```

    Fix any compilation errors, test failures, or integration issues discovered. Common issues to check:
    - Import paths between packages (services/keychain, services/config)
    - Wailsjs stub function signatures matching Go method signatures
    - vi.mock paths at correct depth from __tests__ directories
    - ThreeColumnLayout test needing additional mocks for SettingsDialog
    - StatusBar test needing settingsStore mock

    Verify `go build ./...` succeeds (full compilation including CGO for keyring).
  </action>
  <verify>
    <automated>cd /home/sblanken/working/ppa2 && go build ./... && go test ./... -count=1 2>&1 | tail -20 && cd frontend && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` exits 0
    - `go test ./... -count=1` exits 0
    - `cd frontend && npx vitest run` exits 0
  </acceptance_criteria>
  <done>
    Full project compiles and all tests (Go + frontend) pass with zero failures.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of settings dialog and slash commands</name>
  <files></files>
  <action>
    Present the verification checklist to the user and await confirmation. The app must be started with `wails dev` for interactive testing. All automated tests must pass before this checkpoint.
  </action>
  <verify>User confirms "approved"</verify>
  <done>User has verified settings dialog, slash commands, keychain storage, and clipboard auto-clear all function correctly in the live application.</done>
  <what-built>
    Complete settings dialog with 5 tabs (LLM Config, Prompts, Terminals, Hotkeys, Appearance), all 8 slash commands (/model, /context, /refresh, /filter, /export, /rename, /theme, /help), clipboard auto-clear, and StatusBar live model display.
  </what-built>
  <how-to-verify>
    1. Start the app: `cd /home/sblanken/working/ppa2 && wails dev`
    2. **Settings Dialog:**
       - Click the gear icon in the bottom-right status bar -- settings modal should open
       - Verify all 5 tabs are present and clickable: LLM Config, Prompts, Terminals, Hotkeys, Appearance
       - In LLM Config: select a provider, enter a model name, enter an API key, click Test Connection -- verify inline result appears below button
       - Click Save -- verify StatusBar updates to show the provider:model
       - Close and reopen settings -- verify saved values persist
    3. **Slash Commands:**
       - Type `/help` in chat input -- verify system message listing all commands appears
       - Type `/theme light` -- verify theme switches to light mode
       - Type `/theme dark` -- verify theme switches back to dark
       - Type `/model ollama:llama3` -- verify system message "Model set to ollama:llama3" and StatusBar updates
       - Type `/context 300` -- verify system message "Context set to 300 lines"
       - Type `/refresh` -- verify system message "Terminal content refreshed"
       - Type `/rename myterm` -- verify system message "Tab renamed to myterm"
       - Type `/export json` -- verify system message with file path
    4. **Keychain:** Run `secret-tool lookup service pairadmin account openai` (or whichever provider you configured) -- verify it returns the stored key
    5. **Clipboard Auto-Clear:** Copy a command from the sidebar, wait 60 seconds (or the configured interval), then paste -- verify clipboard is empty
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
```bash
cd /home/sblanken/working/ppa2 && go build ./... && go test ./... -count=1 && cd frontend && npx vitest run
```
</verification>

<success_criteria>
- Full project builds and all tests pass
- Human confirms settings dialog opens and all 5 tabs work
- Human confirms slash commands produce correct output
- Human confirms StatusBar updates with active model
</success_criteria>

<output>
After completion, create `.planning/phases/05-settings-configuration-slash-commands/05-04-SUMMARY.md`
</output>
