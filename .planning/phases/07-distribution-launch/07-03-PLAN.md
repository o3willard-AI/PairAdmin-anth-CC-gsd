---
phase: 07-distribution-launch
plan: 03
type: execute
wave: 3
depends_on:
  - 07-02
files_modified: []
autonomous: false
requirements:
  - DIST-01
  - DIST-02
  - DIST-03
  - DIST-04

must_haves:
  truths:
    - "Clean .deb install on Ubuntu 22.04 launches the application"
    - "Clean .deb install on Ubuntu 24.04 launches the application"
    - "Clean .rpm install on Fedora 40 launches the application"
    - "AppImage limitation is documented if runtime fails"
    - "All v1 requirements checked against live application"
  artifacts: []
  key_links: []
---

<objective>
Human verification of clean install on target distributions and final v1 acceptance criteria check.

Purpose: Validates that packaged artifacts actually work on fresh systems and that all v1 requirements are met before release.
Output: Verified install on Ubuntu 22.04, Ubuntu 24.04, Fedora 40. Final v1 acceptance sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-distribution-launch/07-CONTEXT.md
@.planning/phases/07-distribution-launch/07-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Clean install verification on target distributions</name>
  <files>none — human verification only</files>
  <read_first>
    - .planning/phases/07-distribution-launch/07-CONTEXT.md (D-13 clean install checklist)
    - .planning/phases/07-distribution-launch/07-02-SUMMARY.md (artifact locations and build results)
  </read_first>
  <action>
Present the clean install checklist to the user and wait for verification.

**What was built:** Distribution artifacts: .deb package, .rpm package, AppImage (best-effort), SHA256SUMS, README with install instructions. Per D-13, these must be verified on real/VM systems.

**Clean Install Checklist (per D-13):**

**1. Ubuntu 22.04 (.deb)**
```bash
# Copy .deb to fresh Ubuntu 22.04 VM/container
sudo apt install -y libwebkit2gtk-4.1-0 at-spi2-core
sudo dpkg -i pairadmin_1.0.0_linux_amd64.deb
pairadmin
```
- [ ] dpkg installs without errors
- [ ] `pairadmin` command launches the application window
- [ ] Terminal tabs appear (if tmux running)
- [ ] Can type in chat input
- [ ] Desktop entry appears in application menu

**2. Ubuntu 24.04 (.deb)**
```bash
sudo apt install -y libwebkit2gtk-4.1-0 at-spi2-core
sudo dpkg -i pairadmin_1.0.0_linux_amd64.deb
pairadmin
```
- [ ] dpkg installs without errors
- [ ] Application launches and renders correctly
- [ ] Can connect to Ollama (if running) and complete a chat interaction

**3. Fedora 40 (.rpm)**
```bash
sudo dnf install -y webkit2gtk4.1 at-spi2-atk
sudo rpm -i pairadmin_1.0.0_linux_amd64.rpm
pairadmin
```
- [ ] rpm installs without errors
- [ ] Application launches and renders correctly

**4. AppImage (any Linux)**
```bash
chmod +x pairadmin_1.0.0_linux_amd64.AppImage
./pairadmin_1.0.0_linux_amd64.AppImage
```
- [ ] Either: AppImage launches successfully, OR
- [ ] AppImage fails with WebKit error (expected per Issue #4313) — confirm README documents this limitation

**5. Checksum verification**
```bash
sha256sum --check SHA256SUMS
```
- [ ] All checksums match

**Resume signal:** Type "approved" with pass/fail per distro, or describe issues found.
  </action>
  <verify>
    <automated>echo "checkpoint:human-verify — awaiting user approval"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms .deb installs and launches on Ubuntu 22.04
    - User confirms .deb installs and launches on Ubuntu 24.04
    - User confirms .rpm installs and launches on Fedora 40
    - User confirms AppImage behavior matches documentation (works or fails with documented error)
    - User confirms sha256sum --check SHA256SUMS passes
  </acceptance_criteria>
  <done>Clean install verified on all three target distributions. Checksum verification passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Final v1 acceptance criteria review</name>
  <files>none — human verification only</files>
  <read_first>
    - .planning/REQUIREMENTS.md (all v1 requirements for final check)
    - .planning/phases/07-distribution-launch/07-CONTEXT.md (D-14 final acceptance)
  </read_first>
  <action>
Present the full v1 acceptance criteria to the user for final sign-off. Per D-14, this is the last plan and covers all v1 requirements.

**What was built:** Complete PairAdmin v1.0 application across all 7 phases.

**Final v1 Acceptance Criteria (per D-14):**

Review `.planning/REQUIREMENTS.md` and verify each v1 requirement against the live application:

**Application Shell (SHELL-01 through SHELL-04)**
- [ ] App launches as native desktop window (Wails v2)
- [ ] Three-column layout renders correctly
- [ ] Status bar shows model, connection, tokens
- [ ] Builds on Ubuntu 22.04+ with webkit2_41

**Terminal Capture (TMUX-01 through TMUX-06)**
- [ ] Discovers tmux panes automatically
- [ ] Captures content at 500ms polling
- [ ] New/closed panes handled
- [ ] FNV64a dedup working
- [ ] Isolated per-tab chat

**AT-SPI2 (ATSPI-01 through ATSPI-04)**
- [ ] AT-SPI2 detection and onboarding
- [ ] GNOME Terminal discovery and capture
- [ ] Konsole graceful degradation

**Filtering (FILT-01 through FILT-07)**
- [ ] ANSI stripping, credential redaction
- [ ] Custom filters via /filter command
- [ ] Token count in status bar

**LLM Gateway (LLM-01 through LLM-07)**
- [ ] OpenAI, Anthropic, Ollama, LM Studio providers
- [ ] Streaming with sequence numbers
- [ ] Ollama localhost-only

**Chat (CHAT-01 through CHAT-06)**
- [ ] Send messages, streaming responses
- [ ] Terminal context in system prompt
- [ ] Code blocks with syntax highlighting

**Commands (CMD-01 through CMD-05)**
- [ ] Sidebar with copy-to-clipboard
- [ ] Reverse chronological, clear history

**Clipboard (CLIP-01 through CLIP-03)**
- [ ] Copy to clipboard, Wayland detection, auto-clear

**Config (CFG-01 through CFG-08)**
- [ ] Settings dialog with 5 tabs
- [ ] Keychain storage, Viper persistence

**Slash Commands (SLASH-01 through SLASH-08)**
- [ ] All 8 commands functional

**Security (SEC-01 through SEC-04)**
- [ ] memguard, audit log, response filter, Ollama guard

**Distribution (DIST-01 through DIST-04)**
- [ ] .deb, .rpm, AppImage (best-effort), install-deps.sh

Mark any requirement that does not pass. If all pass, v1.0 is ready for release.

**Resume signal:** Type "v1-approved" if all requirements pass, or list failing requirement IDs with details.
  </action>
  <verify>
    <automated>echo "checkpoint:human-verify — awaiting user v1 approval"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms all v1 requirements pass (or explicitly accepts known limitations)
    - User types "v1-approved" indicating release readiness
  </acceptance_criteria>
  <done>All v1 requirements verified. PairAdmin v1.0 approved for GitHub release.</done>
</task>

</tasks>

<verification>
Human verification only — no automated checks. The checkpoints ARE the verification.
</verification>

<success_criteria>
Clean install verified on Ubuntu 22.04, Ubuntu 24.04, and Fedora 40. All v1 requirements reviewed and passing. Application ready for GitHub release.
</success_criteria>

<output>
After completion, create `.planning/phases/07-distribution-launch/07-03-SUMMARY.md`
</output>
